import 'package:flutter/material.dart';

class VueNotificationMessage extends StatelessWidget{
  String imagePath;
  String pseudo;
  String message;
  String date;
  int nbNotifs;

  VueNotificationMessage({required this.imagePath, required this.pseudo, required this.message, required this.date, required this.nbNotifs});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}